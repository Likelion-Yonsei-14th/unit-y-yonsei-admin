# 릴리스 빌드 백엔드 — `pnpm build`(Vite production mode)가 자동 적용.
# EC2 release 서버. 백엔드 URL 은 비밀이 아니라 git 에 커밋한다.
# ⚠️ 배포 플랫폼(Vercel 등) 대시보드에 VITE_API_BASE_URL 을 따로 설정하지 말 것
#    — 이 파일과 충돌한다. 백엔드 URL 의 진실은 한 곳(이 파일)만.
VITE_API_BASE_URL=https://api.jellyu-yonsei.com/api
