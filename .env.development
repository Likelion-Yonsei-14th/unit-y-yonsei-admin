# 개발 환경 백엔드 — `pnpm dev`(Vite development mode)가 자동 적용.
# Railway 배포본. 백엔드 URL 은 비밀이 아니라 git 에 커밋한다.
# 로컬에서 다른 백엔드(localhost:8080 등)를 쓰려면 .env.local 로 오버라이드.
VITE_API_BASE_URL=https://unit-y-yonsei-server-production.up.railway.app/api
