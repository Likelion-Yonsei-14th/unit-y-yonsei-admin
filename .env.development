# 개발 환경 백엔드 — `pnpm dev`(Vite development mode)가 자동 적용.
# EC2 release 서버를 단일 백엔드로 사용(2026-05-21 부로 Railway 폐기).
# 백엔드 URL 은 비밀이 아니라 git 에 커밋한다.
# 로컬에서 다른 백엔드(localhost:8080 등)를 쓰려면 .env.local 로 오버라이드.
VITE_API_BASE_URL=https://api.jellyu-yonsei.com/api
